// Stub — decorative component removed for MVP performance.
import { memo } from 'react'

function AnimatedBackgroundComponent() {
  return null
}

export const AnimatedBackground = memo(AnimatedBackgroundComponent)
